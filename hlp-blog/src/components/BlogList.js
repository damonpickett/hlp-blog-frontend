// import { Link } from 'react-router-dom'

function BlogList() {
    return(
        <div>
            <h3>Entries</h3>
            {/* I need to make an API call to my backend */}
            {/* It must return a list of all blog entries */}
            {/* The list must show the title and the date of creation */}
            {/* The title needs to link to a Route which shows the content of the post */}

            {/* First goal: get route working */}
            {/* Second goal: have api call return anything from backend */}
            {/* Third goal: api call returns title and created date from backend for all entries */}
        </div>
    )
};

export default BlogList;